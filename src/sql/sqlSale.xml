<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Sale">

	<select id="getDailyOrder" parameterType="string" resultType="orders">
		SELECT o.ORDERNO, m.mName, o.COUNT, o.TOTAL, o.TIME
		FROM
		Orders o , MENU m
		WHERE o.Menuno = m.MENUNO and to_char(o.time, 'YYYYMMDD') = #{value} <!-- to_char(sysdate, 'YYYY-MM-DD') -->
	</select>
	<insert id="dailyClosing">
			
	</insert>

</mapper>
